# NetRecon v1.0 — Python Dependencies
# Install: pip install -r requirements.txt

# SSH enumeration and credential testing
paramiko>=2.11.0

# HTTP enumeration and path probing
requests>=2.28.0

# Note: nmap must be installed separately on your system
# Arch Linux : sudo pacman -S nmap
# Ubuntu/Kali : sudo apt install nmap
# macOS       : brew install nmap
